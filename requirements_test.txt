# Test & lint tooling for ha-airseekers.
# Pin a recent Home Assistant; pytest-homeassistant-custom-component must match the HA version.
homeassistant>=2024.12.0
pytest-homeassistant-custom-component>=0.13.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
ruff>=0.6.0
mypy>=1.10.0

# Optional: used by some discovery tools (tools/) — not required by the integration itself.
# zeroconf
# paho-mqtt
# aiohttp
# python-dotenv
